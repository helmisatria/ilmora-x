# Implementation Plans

Generated by the improve skill on 2026-06-15. Execute in the order below unless dependencies say otherwise. Each executor should read the selected plan fully before starting, honor its STOP conditions, and update the status row when done.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Show Coupon Usage Summaries in Admin Payments | P1 | S | - | DONE |
| 002 | Move Payment Create and Edit Flows into Dialogs | P1 | M | 001 | DONE |
| 003 | Replace Product and Coupon Rows with Admin Tables | P1 | M | 001, 002 | DONE |
| 004 | Improve Checkout, Entitlement, and Manual Grant Support Workflows | P2 | M | 003 | DONE |
| 005 | Add a Persistent Admin Sidebar Shell | P2 | M | 004 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 should land first because Coupon usage data is needed by the Coupon table in 003.
- 002 should land before 003 because table row Edit actions should open dialogs, not write into always-expanded forms.
- 003 should land before 004 because Checkouts and Entitlements should reuse the table shell created for Products and Coupons.
- 005 is last because the sidebar touches the whole admin shell. It is easier to verify after `/admin/payments` has settled.

## Verification baseline from recon

At commit `4f9a420`:

- `pnpm exec tsc --noEmit` passed.
- `pnpm test -- --test-reporter=spec` passed with 24 tests.

## Repo conventions to preserve

- TanStack route files stay focused on loader/head/render wiring.
- Feature behavior belongs under `src/features/*`.
- Cross-cutting UI primitives can live in `src/components/ui`.
- Code should be easy to skim: plain names, early returns, small helpers, no clever one-liners.
- Use the Premium access vocabulary from `CONTEXT.md`: Product, Coupon, Checkout, Entitlement, Premium Membership, Lifetime Try-out Purchase.
- Do not use emoji in admin UI chrome.

## Findings considered and rejected

- Add server-side pagination to `/admin/payments`: deferred. Current loader limits Checkouts and Entitlements to 30 rows and no evidence yet shows this is blocking daily admin work. Revisit when admins need reconciliation or long-history search.
- Add a full finance dashboard: deferred. The request is admin operation and manageability, not revenue analytics.
- Replace all admin pages with tables now: rejected for this pass. Payments is the painful surface; wider admin table extraction should follow after payment patterns prove useful.
